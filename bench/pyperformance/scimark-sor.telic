\ scimark SOR (successive over-relaxation), N=100 — port of pyperformance
\ bm_scimark's SOR kernel. G is a flat row-major vector (idx = y·N + x,
\ exactly Array2D's layout); the stencil updates each interior cell in place.
\
\ G is allocated per outer iteration and left all-zero, as bench_SOR does, so
\ this measures loop throughput and the checksum is 0; the stencil arithmetic on
\ seeded data is checked by tests/089_scimark_kernels.telic. Outer repeat count
\ comes from iterations; the inner relaxation runs cycles sweeps, as in the
\ reference (cycles = 10).

200 to iterations

variable g
100.0 constant n
99.0 constant nm1
10000.0 constant nn
10.0 constant cycles
1.25 0.25 f* constant omega-over-four
1.0 1.25 f- constant one-minus-omega

: build-g ^g |
  nn 1 0-matrix to g ;

: sor-execute
  0 cycles 1 do cycle
     1 nm1 1 do y
        y n f* to rowbase
        1 nm1 1 do x
           rowbase x f+ to idx
           g idx n f- @e
           g idx n f+ @e f+
           g idx 1.0 f- @e f+
           g idx 1.0 f+ @e f+
           omega-over-four f*
           one-minus-omega  g idx @e f*  f+  to nv
           g nv idx !e drop
        loop
     loop
  loop ;

: sor-checksum ( -- s )

  0.0
  0 nn 1 do i0  g i0 @e f+  loop
  to total
  total ;

: sor-bench
  now to t0
  0 iterations 1 do r  build-g sor-execute  loop
  now t0 - to t0
  "elapsed: " . t0 . " s" . cr
  sor-checksum "checksum: {0}" format . cr ;

sor-bench
bye
