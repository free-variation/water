\ scimark SparseMatMult — port of pyperformance bm_scimark's sparse matrix x
\ vector kernel. A compressed-row sparse matrix (N rows, NR nonzeros/row) times a
\ dense vector, repeated cycles times. The row/col sparsity structure matches the
\ reference exactly, and val and x are left zero as bench_SparseMatMult leaves
\ them, so this measures loop throughput and the checksum (sum of the result
\ vector) is 0; the matvec arithmetic on seeded data is checked by
\ tests/089_scimark_kernels.telic. Build is untimed; only the cycles sweeps are
\ measured.

1000 to cycles

variable row  variable col  variable val  variable x  variable y
1000.0 constant n
50000.0 constant nz
50.0 constant nr
50000.0 constant anz

: build-sparse ^row ^col ^val ^x ^y |
  n 1.0 f+ 1 0-matrix to row
  nz 1 0-matrix to col
  anz 1 0-matrix to val
  n 1 0-matrix to x
  n 1 0-matrix to y
  row 0.0 0.0 !e drop
  0 n 1 do r
     row r @e to rowr
     row  rowr nr f+  r 1.0 f+  !e drop
     r nr / ftruncate to step
     step 1.0 < if 1.0 to step then
     0 nr 1 do k
        col  k step f*  rowr k f+  !e drop
     loop
  loop ;

: sparse-matmult
  0 cycles 1 do c
     0 n 1 do r
        row r @e to lo
        row r 1.0 f+ @e to hi
        0.0
        lo hi 1 do i0
           x col i0 @e @e  val i0 @e f*  f+
        loop
        y swap r !e drop
     loop
  loop ;

: sparse-checksum ( -- s )

  0.0
  0 n 1 do i0  y i0 @e f+  loop
  to total
  total ;

: sparse-bench
  build-sparse
  now to t0
  sparse-matmult
  now t0 - to t0
  "elapsed: " . t0 . " s" . cr
  sparse-checksum "checksum: {0}" format . cr ;

sparse-bench
bye
